<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="GoodsMapper">
	
	<!--表名 -->
	<sql id="tableName">
		PO_GOODS
	</sql>
	
	<!--数据字典表名 -->
	<sql id="dicTableName">
		SYS_DICTIONARIES
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		f.GOODS_NAME,	
		f.GOODS_PRICE,	
		f.GOODS_NUMBER,	
		f.GOODS_DESCRIBE,	
		f.GOODS_STATE,	
		f.GOODS_PICTURE,	
		f.GOODS_ID
	</sql>
	
	<!-- 字段用于新增 -->
	<sql id="Field2">
		GOODS_NAME,	
		GOODS_PRICE,	
		GOODS_NUMBER,	
		GOODS_DESCRIBE,	
		GOODS_STATE,	
		GOODS_PICTURE,	
		GOODS_ID
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{GOODS_NAME},	
		#{GOODS_PRICE},	
		#{GOODS_NUMBER},	
		#{GOODS_DESCRIBE},	
		#{GOODS_STATE},	
		#{GOODS_PICTURE},	
		#{GOODS_ID}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field2"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			GOODS_ID = #{GOODS_ID}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			GOODS_NAME = #{GOODS_NAME},
			GOODS_PRICE = #{GOODS_PRICE},
			GOODS_NUMBER = #{GOODS_NUMBER},
			GOODS_DESCRIBE = #{GOODS_DESCRIBE},
			GOODS_STATE = #{GOODS_STATE},
			<if test="GOODS_PICTURE!=null and GOODS_PICTURE!=''">GOODS_PICTURE = #{GOODS_PICTURE},</if>			
			GOODS_ID = GOODS_ID
		where 
			GOODS_ID = #{GOODS_ID}
	</update>
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 
			f.GOODS_ID = #{GOODS_ID}
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
		where 1=1
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include> f
	</select>
	
	<!-- 批量上架 -->
	<update id="editAlls" parameterType="String">
		update
		<include refid="tableName"></include>
		set
			GOODS_STATE = "上架"
		where
			GOODS_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>	
	</update>
	
	<!-- 批量下架 -->
	<update id="editAllx" parameterType="String">
		update
		<include refid="tableName"></include>
		set
			GOODS_STATE = "下架"
		where
			GOODS_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>	
	</update>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			GOODS_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- fh313596790qq(青苔) -->
</mapper>